---
tags:
  - linguagem-de-programacao
  - python
  - básico
  - ambiente-virtual
  - venv
links: https://docs.python.org/3/tutorial/venv.html
---
# Ambiente Virtual (VENV)

**É recomendado criar um ambiente virtual por projeto** para isolar ambientes, evitando conflitos entre bibliotecas de diferentes versões que você usa nos projetos pois ele isola as bibliotecas que você instala dentro do ambiente ativo.

Dentro do diretório desejado, execute:

``` bash
python -m venv nome-do-env # cria ambiente virtual
```

O local mais comum de criar esse ambiente virtual é o `.venv` para evita colidir com variáveis de ambiente do `.env`, que costuma ser adicionado no `.gitignore` para não ser enviado para o repositório remoto.

Com o ambiente criado, agora ative ele com o seguinte comando:

``` bash
nome-do-env\Scripts\activate # Windows

source nome-do-env/bin/activate # Unix ou MacOS
```

Isso vai mudar seu terminal que agora estará apontando para este novo ambiente e todas as bibliotecas instaladas serão instaladas dentro deste ambiente, podendo usar até mesmo uma versão diferente de _Python_ em cada ambiente.

``` python
$ .venv\nome-do-env\Scripts\activate
(nome-do-env) $ python
Python 3.5.1 (default, Jul  7 2026, 14:10:36)
  ...
>>> import sys
>>> sys.path
['', '/usr/local/lib/python35.zip', ...,
'~/.venv/nome-do-env/lib/python3.5/site-packages']
>>>
```

Isso não altera a variável `PYTHONPATH`, o que pode levar a possíveis problemas caso o _path_ inclua referência com uma versão do _Python_ incompatível com a versão que o ambiente virtual está usando. A melhor prática é usar `unset PYTHONPATH` no terminal que você estiver usando.

Para desativar o ambiente virtual, digite no terminal:

``` bash
deactivate
```
